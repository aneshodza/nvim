-- solargraph is a ruby gem, not a mason package, so it is listed in
-- chadrc.mason.skip and has to be installed with `gem install solargraph`.
return {
  settings = {
    solargraph = { diagnostics = true },
  },
}
